using UnityEngine;

public class GrabAndMove : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log(Camera.main.transform.foward); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
